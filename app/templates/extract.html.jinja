{% extends "base.html.jinja" %}
{% block content %}
    <h2>Pobierz opinie o produkcie</h2>
    <form action="{{url_for('extract')}}"
    <div class="row g-3 align-items-center">
        <div class="col-auto">
            <label for="product_id" class="col-form-label">Kod produktu</label>
        </div>
        <div class="col-auto">
            <input type="product_id" id="product_id" name="product_id" class="form-control">
        </div>
        <div class="col-auto">
            <input type="submit" class="btn btn-primary" value="Pobierz">
        </div>
    </div>
{% endblock content %}